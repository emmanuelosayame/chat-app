import { Box } from "@chakra-ui/react";
import { NextPage } from "next";
import { auth } from "../firebase/firebase";

const Home: NextPage = () => {
  // auth.signOut();

  return (
    <>
      <Box>hii</Box>
    </>
  );
};

export default Home;
